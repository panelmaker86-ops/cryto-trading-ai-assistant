# Use dummy data instead of the backend API (e.g. when only running the dashboard).
# Set to "true" to always show mock data and display "Connected" in the UI.
# Leave unset: dashboard will try the API and fall back to mock data if the API is down.
VITE_USE_MOCK=true
